info:
  name: api/v1/client-portal/appointments/companies
  type: http
  seq: 1

http:
  method: GET
  url: "{{belezapro-api}}/api/v1/client-portal/appointments/companies"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
