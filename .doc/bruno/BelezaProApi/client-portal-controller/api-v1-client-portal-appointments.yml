info:
  name: api/v1/client-portal/appointments
  type: http
  seq: 2

http:
  method: GET
  url: "{{belezapro-api}}/api/v1/client-portal/appointments?page=0&size=15"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
