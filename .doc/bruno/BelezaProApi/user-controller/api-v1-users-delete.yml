info:
  name: api/v1/users/USER_ID
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{belezapro-api}}/api/v1/users/USER_ID"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
