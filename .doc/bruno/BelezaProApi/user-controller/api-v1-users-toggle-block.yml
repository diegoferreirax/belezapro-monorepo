info:
  name: api/v1/users/USER_ID/toggle-block
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{belezapro-api}}/api/v1/users/USER_ID/toggle-block"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
