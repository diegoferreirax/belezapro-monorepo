info:
  name: api/v1/users/USER_ID
  type: http
  seq: 3

http:
  method: PUT
  url: "{{belezapro-api}}/api/v1/users/USER_ID"
  auth:
    type: bearer
    token: "{{token}}"
  body: json

body:json:
  {
  "name": "Admin Updated",
  "role": "ADMIN",
  "password": "123"
}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
