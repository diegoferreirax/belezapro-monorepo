info:
  name: api/v1/expenses (list)
  type: http
  seq: 1

http:
  method: GET
  url: "{{belezapro-api}}/api/v1/expenses?month=4&year=2026"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
