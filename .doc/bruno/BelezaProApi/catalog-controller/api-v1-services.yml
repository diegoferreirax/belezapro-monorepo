info:
  name: api/v1/services
  type: http
  seq: 1

http:
  method: GET
  url: "{{belezapro-api}}/api/v1/services"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
