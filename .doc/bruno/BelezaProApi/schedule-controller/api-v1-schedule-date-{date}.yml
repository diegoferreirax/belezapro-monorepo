info:
  name: api/v1/schedule/date/{date}
  type: http
  seq: 5

http:
  method: GET
  url: "{{belezapro-api}}/api/v1/schedule/date/2026-04-08"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
