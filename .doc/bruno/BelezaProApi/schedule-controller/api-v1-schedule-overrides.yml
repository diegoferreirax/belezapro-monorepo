info:
  name: api/v1/schedule/overrides
  type: http
  seq: 14

http:
  method: GET
  url: "{{belezapro-api}}/api/v1/schedule/overrides"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
