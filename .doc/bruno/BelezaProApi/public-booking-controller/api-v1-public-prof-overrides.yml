info:
  name: api/v1/public/professionals/PROF_ID/schedule/overrides
  type: http
  seq: 18

http:
  method: GET
  url: "{{belezapro-api}}/api/v1/public/professionals/{{professional-id}}/schedule/overrides"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
