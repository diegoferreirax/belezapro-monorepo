info:
  name: api/v1/clients/CLIENT_ID/toggle-block
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{belezapro-api}}/api/v1/clients/CLIENT_ID/toggle-block"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
