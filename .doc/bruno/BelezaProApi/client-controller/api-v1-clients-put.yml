info:
  name: api/v1/clients/CLIENT_ID
  type: http
  seq: 3
  docs: PUT atualiza nome e telefone. Corpo = UpdateClientRequest (name, phone). Email não é alterado.

http:
  method: PUT
  url: "{{belezapro-api}}/api/v1/clients/CLIENT_ID"
  auth:
    type: bearer
    token: "{{token}}"
  body: json

body:json:
  {
  "name": "Cliente Editado",
  "phone": "44888888"
}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
