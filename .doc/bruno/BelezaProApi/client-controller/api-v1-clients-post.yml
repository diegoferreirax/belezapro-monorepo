info:
  name: api/v1/clients
  type: http
  seq: 2

http:
  method: POST
  url: "{{belezapro-api}}/api/v1/clients"
  auth:
    type: bearer
    token: "{{token}}"
  body: json

body:json:
  {
  "name": "Cliente Novo",
  "email": "teste@cliente.com",
  "phone": "449999999"
}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
