info:
  name: api/v1/appointments/range
  type: http
  seq: 10

http:
  method: GET
  url: "{{belezapro-api}}/api/v1/appointments/range?startDate=2026-01-01&endDate=2026-12-31"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
