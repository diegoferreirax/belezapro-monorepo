info:
  name: api/v1/appointments/active/date/2026-04-10
  type: http
  seq: 11

http:
  method: GET
  url: "{{belezapro-api}}/api/v1/appointments/active/date/2026-04-10"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
